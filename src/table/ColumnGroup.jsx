import React, {Component} from 'react';
import PropTypes from 'prop-types';

export default class ColumnGroup extends Component {
    static propTypes = {
        title: PropTypes.node
    };
    static __FY_TABLE_COLUMN_GROUP = true;
}
